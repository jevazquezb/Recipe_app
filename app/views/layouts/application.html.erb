<!DOCTYPE html>
<html>
  <head>
    <title>ReceipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous"> 
  </head>

  <body> 
    <nav class="navbar bg-light">
      <div class="container-fluid">
      <div class="d-flex">
          <% if current_user %>
            <a href="/recipes" class="navbar-brand btn btn-outline-primary">Recipe's List</a>
            <a href="/recipes/create" class="navbar-brand btn btn-outline-primary">Add Recipe</a>
            <a href="/foods" class="navbar-brand btn btn-outline-primary">Food's List</a>
            <a href="/foods/new" class="navbar-brand btn btn-outline-primary">Add Food</a>
          <%end%>
        </div>
        <div class="d-flex">
        <% if current_user %>
          <a href="/users/sign_out">
          <button class="btn btn-outline-danger" >log out</button>
          </a>
        <% else %>
          <a href="/users/sign_in">
          <button class="btn btn-outline-success" >log in</button>
          </a>
        <%end%>
        </div>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
